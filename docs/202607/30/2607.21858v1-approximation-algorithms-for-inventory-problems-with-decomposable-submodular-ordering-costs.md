---
title: Approximation Algorithms for Inventory Problems with Decomposable Submodular Ordering Costs
title_zh: 可分解次模订购成本下库存问题的近似算法
authors: "Retsef Levi, Georgia Perakis, Emily Zhang"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21858v1"
tags: ["query:wo"]
score: 9.0
evidence: 具有次模订购成本的联合补货问题
tldr: 联合补货问题在物流库存管理中至关重要，需协调多物品补货以最小化总持有与订购成本，其中子模订购成本捕捉了复杂订购交互。本文针对可分解为k个类别的子模成本，提出基于线性规划松弛与边际成本注水划分的舍入算法，构造可行整数调度。该算法实现O(k)近似比，当类别数固定时首次获得常数因子保证，显著扩展了可求解的子模成本函数族。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21858v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1674, \"height\": 1916}, {\"url\": \"assets/figures/arxiv/2607.21858v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1686, \"height\": 513}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21858v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1691, \"height\": 708}]"
motivation: 现有联合补货问题研究缺乏对广泛可分解子模订购成本的近似算法。
method: 通过线性规划松弛，结合基于注水过程的边际成本区域划分与舍入获得整数解。
result: 算法达到O(k)近似比，当类别数固定时获得首个常数近似保证。
conclusion: 为库存物流中的联合补货问题建立了新的近似界，拓展了可处理的成本函数类别。
---

## 摘要
本文针对一大类可分解次模订购成本函数下的次模联合补货问题（SJRP），设计了一种近似算法。在 SJRP 中，中央决策者在有限离散规划周期内协调订单以满足多种物品的确定性需求，同时最小化总持有成本与订购成本，其中订购成本被建模为各周期订购物品子集的次模函数。本文考虑的订购成本函数基于将物品分解为 k 个类别来定义，成本是各类别内加权汇总量的函数，并通过一个联合成本函数允许类别间的任意交互。所提出的算法通过一个新颖的注水过程，将线性规划松弛的解根据边际成本划分为嵌套区域，然后从每个区域选择一个订单，从而获得可行的整数排程。该算法达到 O(k)-近似比。当类别数 k 固定时，这首次为这一类广泛的次模订购成本提供了常数因子保证，显著扩展了已知具有此类保证的成本函数类别。

## Abstract
This paper develops an approximation algorithm for the submodular joint replenishment problem (SJRP) under a broad family of decomposable submodular ordering cost functions. In the SJRP, a central planner coordinates orders to satisfy deterministic demand for multiple items over a finite discrete planning horizon while minimizing total holding and ordering costs, with the latter modeled as a submodular function of the subset of items ordered in each period. The ordering cost functions considered in this paper are defined based on a decomposition of the items into $k$ categories, where the cost is a function of weighted aggregate quantities within each category and allows for arbitrary interactions across categories through a joint cost function. The proposed algorithm rounds the solution to a linear programming relaxation by partitioning the fractional solution into nested regions according to marginal costs using a novel water-filling procedure, and then selecting one order from each region to obtain a feasible integral schedule. The resulting algorithm achieves an $O(k)$-approximation. When the number of categories $k$ is fixed, this yields the first constant-factor guarantee for this broad class of submodular ordering costs, significantly expanding the class of cost functions for which such guarantees are known.

---

## 论文详细总结（自动生成）

好的，作为一名资深交通运输工程研究者，我将以规范的交通运输专业术语，对给定的论文进行结构化、深入、客观的总结。

### 1. 研究问题与交通场景
*   **研究问题**: 论文研究的是**次模联合补货问题（Submodular Joint Replenishment Problem, SJRP）** 的近似算法。核心挑战在于，在满足多物品确定性需求的前提下，如何协调订单时间与物品组合，以最小化总持有成本与复杂的联合订购成本。
*   **运输方式与场景**: 该问题可以直接映射到**公路货运**与**城市物流配送**场景。典型应用为**库存路径问题（Inventory Routing Problem, IRP）**，即一个中央仓库（配送中心）需要为多个地理分散的客户（需求点）制定补货与车辆路径计划。
*   **空间尺度与参与者**: 空间尺度为**区域/城市级**的多客户配送网络。主要参与者包括一个**中心决策者（如物流运营商）** 和多个**需求点（如零售商、加油站）**。
*   **供需关系与工程背景**: 需求是**确定性的、动态的**，即各物品在各周期有已知的、必须按时满足的需求量。工程背景是**供应链协同**与**运输调度优化**，旨在利用联合运输的规模经济效应，解决“频繁小批量运输（持有成本低但运输成本高）”与“少次大批量运输（运输成本低但持有成本高）”之间的经典权衡。订购成本被建模为**次模函数**，能灵活捕捉共享资源（如卡车、仓储、劳动力）的复杂成本结构，例如，增加一个配送点至已有路线，其额外成本低于单独为其开辟一条新路线。

### 2. 理论与方法
*   **核心理论与模型**: 论文基于**运筹优化**理论，将该库存问题形式化为一个**整数规划模型 (IP)**。该模型的目标函数为最小化总订购成本与总持有成本之和，约束条件确保所有需求必须在截止时间前被满足。
*   **关键假设与变量**:
    *   **订购成本函数**: 是核心创新点，被定义为一种**可分解次模函数**。具体而言，所有物品被划分为 *k* 个不相交的类别。订单成本 $K(S) = f(\mathbf{u}(S))$，其中 $\mathbf{u}(S)$ 是各类别物品加权求和后形成的 *k* 维向量，而 $f$ 是一个连续、可微的 **DR-次模函数**，允许任意跨类别交互。
    *   **决策变量**: $y^S_s$ 表示是否在周期 $s$ 订购物品子集 $S$（0-1变量）；$x^i_{sd}$ 表示需求点 $(i, d)$ 是否由周期 $s$ 的订单满足（0-1变量）。
*   **算法流程:**
    1.  **问题归约**: 首先将复杂的 **SJRP** 归约为一个更简单的 **不相交区间覆盖问题 (Disjoint Interval Covering Problem, DICP)**，该问题消除了持有成本，聚焦于用最小订购成本覆盖一系列不相交的需求时间窗。此归约过程仅造成常数倍的近似比损失。
    2.  **求解线性规划松弛**: 求解 **DICP** 的线性规划松弛模型，得到一个分数解。
    3.  **核心舍入算法（针对 DICP）**:
        *   **定义边际成本（级别）**: 为每个物品子集定义一个**物品类别相关的“级别” ($j$-level)**，该级别近似于向该子集中加入对应类别物品的边际成本。
        *   **注水过程分区**: 独创性地使用**注水过程**，在时间-订单数量二维平面上，按照级别从低到高的顺序“填充”分数解所代表的区域，直至填充面积为 1/2，从而将分数解递归地、逐层地分割成多个面积均为 1/4 的**层级集合**。每次分割都将时间轴一分为二。
        *   **动态规划选单**: 基于分割形成的层级集合，构建一个树状结构，并采用**动态规划**在每个层级集合内恰选择1个订单，以最小化总成本满足所有片区需求。选单原则是如果一个订单在其时间点覆盖了一个节点，它也覆盖了该节点下的一条路径。
    4.  **合并排程**: 将针对 *k* 个类别独立求解的订单排程合并，形成最终的整数排程。

### 3. 数据与实验设计
*   本研究为**纯理论研究**，不涉及具体的实证数据或仿真实验。论文通过严格的数学推导和证明来验证算法的正确性和性能。因此，不存在传统意义上的数据来源、样本规模、网络场景或对比方法的描述。实验验证被**理论证明**所替代。

### 4. 评价指标与结果
*   **评价指标**: 算法的性能由**理论近似比**度量，即算法产生的解的成本与理论最优解成本之间的最大比值。
*   **主要结果**:
    *   所提出的算法针对 **SJRP** 实现了 **$(8k+1)$-近似比**，针对 **DICP** 子问题实现了 **$4k$-近似比**。
    *   其中，$k$ 是物品类别数量。当 $k$ 为固定常数时，这是对该广泛类别次模订购成本问题的**首个常数因子近似保证**，是一个重大的理论突破。

### 5. 有效性检验
*   本文的检验方式是**理论证明**，而非实证检验。
*   **算法可行性**: 通过 **Lemma 3** 和 **Lemma 4** 等引理，严谨证明了舍入算法生成的整数解对于 **DICP** 是可行的，即所有需求区间均被覆盖。
*   **成本上界证明**: 通过 **Proposition 2** 和 **Proposition 3** 等命题，并结合一个巧妙的随机算法分析，严格证明了算法输出的订购成本不超过线性规划松弛最优解的 $4k$ 倍。这是理论计算机科学中标准的“分析”范式，其稳健性内嵌于最坏情况保证中。

### 6. 主要结论与交通工程启示
*   **主要结论**: 论文成功地将 **SJRP** 的常数近似保证从少数特殊成本结构（如树状、层状、基数成本）扩展到了一个**尺度更广的、具有任意跨类别交互的可分解次模成本函数族**。
*   **交通工程启示**:
    *   **更优的物流规划**: 为复杂的协同库存与运输规划问题提供了理论上的性能保证，可指导设计更高效、更具成本效益的补货策略。
    *   **灵活的建模能力**: 所提出的可分解次模成本结构允许建模现实中复杂的共享成本交互，如不同类别的商品（温控品、干货）共用一辆具有多维容量限制的车辆时产生的复杂成本。这种建模能力可直接用于解决更贴近现实的**库存路径问题 (IRP)**。
    *   **算法设计启示**: 研究中发展的“边际成本注水划分”和“动态规划选单”方法，为求解其他带复杂组合成本结构的交通与物流优化问题提供了新的思路。

### 7. 优点与创新
*   **理论创新性强**: 首次为解决一类广泛的、允许任意跨类别交互的次模联合补货问题提供了常数因子近似算法，解决了领域内一个长期悬而未决的问题。
*   **方法论创新**: 提出了新颖的“注水”分区与动态规划相结合的舍入技术，其结构性洞察（如 Lemma 3 对物品包含性的刻画）极具巧思和启发性。
*   **模型表达力强**: 所研究的次模成本函数分解框架比现有研究的成本结构（如简单的可加成本）强大得多，能更好地捕捉物流实践中的规模经济与范围经济效应。
*   **问题归约简洁**: 将无持有成本的特殊情况 **DICP** 作为子问题，并能通过只损失常数倍的优雅方式归约原问题 **SJRP**，分离了问题难点。

### 8. 局限与适用边界
*   **成本函数的局限性**: 算法仅适用于论文**精确定义的、基于类别划分的可分解次模函数族**。对于通用的、不满足此分解性质的次模联合补货问题，以及一般度量空间下的库存路径问题，该算法不适用，其常数近似仍为开放问题。
*   **理论近似的紧致性**: 虽然获得了 $O(k)$ 近似比，但该理论界是否紧致（即是否存在最坏情况实例逼近此上界）有待进一步研究。
*   **计算复杂性**: 虽然算法是多项式时间的，但其依赖于使用椭球法求解具有指数级变量的线性规划及其对偶，这可能在实际大规模问题中计算代价高昂。论文未提供真实计算时间的评估。
*   **模型假设**: 模型假设需求是确定性的，且订购成本仅依赖于订购的物品子集，与订购量无关。在需求不确定或成本与订购量强相关的场景下，模型需要进一步扩展。
*   **缺乏实证测试**: 作为一篇理论论文，并未在真实数据集或仿真环境上测试算法的实际性能与运行时间，其工程实用性有待后续研究验证。

（完）
