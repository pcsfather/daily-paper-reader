---
title: Reoptimization Algorithms for Contextual Bandits with Knapsack Constraints
title_zh: 带背包约束的上下文老虎机重优化算法
authors: Zhen Xu
date: 2026-08-11
pdf: "https://arxiv.org/pdf/2608.11383v1"
tags: ["query:wo"]
score: 8.0
evidence: 资源受限下基于未知需求的在线产品分配，直接支持基于需求的库存管理策略
tldr: 在客户、产品和资源类型有限且资源容量受限的在线决策中，决策者需为每位到达客户分配产品，并获得由客户与产品特征线性决定且含噪声的随机奖励。目标为同时学习未知奖励函数并优化在线分配以最小化期望收益损失。本文提出上置信界算法的自然扩展，并引入重优化技术。算法实现平均遗憾 O((ln T)^3/T)，较现有基于重优化的动态定价方法 O(1/sqrt(T)) 显著降低，证明重优化能有效提升学习效率，为容量受限在线决策提供更紧遗憾界。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.11383v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1663, \"height\": 2350, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.11383v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1693, \"height\": 508, \"label\": \"Table\"}]"
motivation: 现有基于重优化的动态定价方法平均遗憾仅达 O(1/sqrt(T))，需更高效算法以降低容量受限在线学习的收益损失。
method: 提出UCB算法自然扩展，结合重优化技术，在线学习未知线性奖励函数并更新资源容量约束。
result: 算法平均遗憾为 O((ln T)^3/T)，显著优于文献中 O(1/sqrt(T)) 的界。
conclusion: 重优化能大幅降低遗憾，为容量受限的在线资源分配提供高效学习策略。
---

## 摘要
我们研究了带背包约束的上下文老虎机问题的新算法。在这类问题中，顾客、产品和资源的类型均为有限多个。每种产品由固定的资源组合构成，且资源的容量有限。决策者必须为每位到达的顾客从多个可能产品中分配一种。每次将顾客分配给一种产品都会产生随机收益，该收益等于顾客和产品特征的未知线性函数加上噪声项。目标是同时学习平均收益函数并进行在线分配，以最小化相对于已知收益函数的最优策略的期望收益损失。我们提出了上置信界（UCB）算法族的一个自然且简单的扩展，并应用重优化技术。我们证明，通过利用重优化，我们的算法达到了 $O(\frac{(\ln T)^3}{T})$ 的平均遗憾，其中 $T$ 为时间范围长度。我们的界显著降低了文献中基于重优化的密切相关的动态定价问题的 $O(\frac{1}{\sqrt{T}})$ 界。

## Abstract
We study new algorithms for Contextual Bandits with Knapsack. In these problems, there are finitely many types of customers, products, and resources. Each product is made from a fixed combination of resources, and resources have finite capacity. A decision maker must assign each arriving customer one out of a set of multiple possible products. Every assignment of a customer to a product will generate a random reward, which equals an unknown linear function of customer and product features, plus a noise term. The objective is to jointly learn the mean reward function, and to make online assignments to minimize the expected revenue loss relative to an optimal policy that knows the reward function. We propose a natural and simple extension of the Upper-Confidence-Bound (UCB) family of algorithms and apply re-optimization techniques. We show that by taking advantage of re-optimization, our algorithm achieves an average regret of $O(\frac{(\ln T)^3}{T})$ where $T$ is the horizon length. Our bound significantly reduces the $O(\frac{1}{\sqrt{T}})$ bound in the literature for closely related dynamic-pricing problems that are based on re-optimization.

---

## 论文详细总结（自动生成）

## 1. 研究问题与交通场景

- **问题性质**：论文研究的是“带背包约束的上下文老虎机”（Contextual Bandits with Knapsacks, CBwK）问题，属于运筹优化与在线学习的交叉领域。
- **可类比的交通场景**：论文并非基于真实城市交通数据的案例研究，但其模型可映射到出行平台、网约车调度、共享运力分配等交通工程场景。文中明确提到 ride-hailing 平台作为动机之一。
- **运输方式与空间尺度**：不限定具体运输方式；抽象层面适用于平台级、城市级或网络级的容量受限资源分配，而不是路段级交通流。
- **参与者与供需关系**：
  - **供给方**：平台拥有多种产品（如拼车、专车、快车、公交接驳等），每种产品消耗固定数量的资源（如司机服务时长、车辆座位、停车容量、充电桩容量、广告预算等）。
  - **需求方**：异质顾客按随机概率到达，顾客类型具有不同特征。
  - **决策者**：平台在每个时段为到达顾客分配一个产品，或拒绝服务。
- **工程背景**：平台需要在高频在线环境中同时学习未知的顾客-产品匹配收益，并在有限资源约束下最大化长期收益。与交通系统相关的问题包括网约车实时派单、共享出行容量管理、动态定价、充电资源预留等。

## 2. 理论与方法

### 2.1 总体方法框架

- 论文将 **线性上下文老虎机** 与 **网络收益管理中的重优化线性规划** 结合，提出一种 **上置信界（UCB）引导的重优化算法**。
- 每期通过求解一个 **双线性规划**，在乐观收益下决定分配概率；再根据实际资源消耗更新剩余容量，并在下一期重新求解。
- 理论上采用 **流体线性规划基准**、**置信椭球**、**鞅分析** 和 **停时分析** 给出遗憾界。

### 2.2 关键假设

- 顾客类型为有限集合，每个时段至多到达一个顾客，到达概率 \(\lambda_i\) 已知。
- 产品特征矩阵 \(Y\) 和顾客特征矩阵 \(X\) 已知。
- 奖励为未知线性函数：
  \[
  r = U_i^\top Y_j + \xi,
  \]
  其中 \(U_i\) 是未知收益参数，\(\xi\) 为零均值、有界噪声。
- 每种产品对资源的消耗量 \(a_{jk}\) 固定且已知。
- 资源总容量为 \(B_k\)，容量规模与时间范围 \(T\) 成比例。
- **Assumption 1**：基准线性规划存在唯一对偶最优解，且容量约束对应的对偶变量严格为正。这是保证容量小扰动下最优解连续变化的关键假设。

### 2.3 核心变量与公式

- **决策变量**：\(x_{ijt}\) 表示在时段 \(t\) 将类型 \(i\) 顾客分配给产品 \(j\) 的概率。
- **容量状态**：\(b_{kt}\) 为时段 \(t\) 的实际可用平均容量，初始为 \(B_k/T\)。
- **学习状态**：\(\hat U_{it}\) 为 \(U_i\) 的最小二乘估计，\(M_{it}\) 为相应样本协方差矩阵。
- **置信球**：
  \[
  CB_{it}=\{v:\|v-\hat U_{it}\|_{2,M_{it}}\le \beta_t\}.
  \]
- **每期重优化问题**：
  \[
  x_t=\arg\max_{x\in D_t}\max_{V\in CB_t}\sum_{i,j}x_{ij}V_i^\top Y_j,
  \]
  其中 \(D_t\) 为当前剩余容量和到达概率约束下的可行域。
- **容量更新规则**：
  \[
  b_{k,t+1}=b_{kt}-\frac{d_{kt}-b_{kt}}{T-t},
  \]
  其中 \(d_{kt}\) 为时段 \(t\) 的实际资源消耗。
- **估计更新**：按标准岭回归或最小二乘更新 \(M_{it}\) 和 \(\hat U_{it}\)。

### 2.4 算法流程

1. 初始化平均容量、单位矩阵 \(M_{i1}\)、估计值 \(\hat U_{i1}=0\)。
2. 每个时段构造置信球。
3. 求解带有乐观收益的线性规划，得到分配概率 \(x_t\)。
4. 对到达顾客按 \(x_{ijt}/\lambda_i\) 的概率分配产品；若资源不足则拒绝。
5. 观察奖励，更新 \(M_{it}\) 和 \(\
